BOT_TOKEN=your_bot_token_here
MASTER_KEY=your_litellm_master_key
LITELLM_BASE_URL=https://litellm.sam.co.th
# DB_PATH is hardcoded in Dockerfile as /app/data/bot.db
# Do NOT set in .env — the named volume (bot-data) mounts at /app/data.
# Changing DB_PATH will cause data loss on every deploy.

